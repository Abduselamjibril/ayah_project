// app/binding.dart
// Simple service initialization without GetX
class AppBinding {
  static void initialize() {
    // Initialize your services, databases, etc. here if needed
    // This can be called in main() or in your App widget

    // Example:
    // await DatabaseService.initialize();
    // AudioService.initialize();
    // etc.
  }
}
