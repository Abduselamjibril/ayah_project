Here's the updated `main.dart` file with database initialization:

```dart
// main.dart
import 'package:flutter/material.dart';
import 'package:quran_app/app/app.dart';
import 'package:quran_app/core/database/init_database.dart';

Future<void> main() async {
  // Ensure Flutter widgets are initialized
  WidgetsFlutterBinding.ensureInitialized();
  
  // Initialize database and seed initial data
  await DatabaseInitializer.initializeDatabase();
  
  // Run the app
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Quran App',
      theme: ThemeData(
        primarySwatch: Colors.green,
        useMaterial3: false, // As recommended by quran_library package
        colorScheme: ColorScheme.fromSeed(
          seedColor: Colors.green,
          brightness: Brightness.light,
        ),
        appBarTheme: AppBarTheme(
          backgroundColor: Colors.green[700],
          foregroundColor: Colors.white,
          elevation: 2,
        ),
        scaffoldBackgroundColor: const Color(0xFFF5F5DC), // Parchment-like background
      ),
      darkTheme: ThemeData(
        primarySwatch: Colors.green,
        useMaterial3: false,
        colorScheme: ColorScheme.fromSeed(
          seedColor: Colors.green,
          brightness: Brightness.dark,
        ),
        appBarTheme: const AppBarTheme(
          backgroundColor: Color(0xFF1B5E20), // Dark green
          foregroundColor: Colors.white,
          elevation: 2,
        ),
        scaffoldBackgroundColor: const Color(0xFF121212),
      ),
      themeMode: ThemeMode.light, // You can change this to system or dark
      home: const App(), // Your main app widget
      debugShowCheckedModeBanner: false,
    );
  }
}
```

## Important Changes Made:

1. **Added `async` to main function**: To allow for database initialization before running the app.

2. **Database Initialization**: Added `DatabaseInitializer.initializeDatabase()` call before `runApp()`.

3. **Theme Updates**:
   - Changed primary color from blue to green (more appropriate for Quran app)
   - Set `useMaterial3: false` as recommended by the quran_library package
   - Added parchment-like background color (#F5F5DC)
   - Added dark theme support
   - Customized AppBar theme

4. **ThemeMode**: Set to `ThemeMode.light` by default, but you can change it to:
   - `ThemeMode.system` - Follows system theme
   - `ThemeMode.dark` - Always dark theme
   - `ThemeMode.light` - Always light theme (current)

## Note:
Make sure you have created the `DatabaseInitializer` class as shown in the previous response. If you haven't created it yet, here's a simplified version that you can use:

```dart
// lib/core/database/init_database.dart
import 'package:sqflite/sqflite.dart';
import 'app_database.dart';

class DatabaseInitializer {
  static Future<void> initializeDatabase() async {
    try {
      // This will create/initialize the database
      final database = await AppDatabase.instance.database;
      
      // You can add initial data seeding here
      print('Database initialized successfully');
    } catch (e) {
      print('Error initializing database: $e');
    }
  }
}
```

This initialization ensures that your database is ready before the app starts, which is crucial for tafsir and translation features to work properly.